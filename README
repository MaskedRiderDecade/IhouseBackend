添加了拦截器 分别拦截/admin 和/* 的内容
不包括user的登录 注册 搜索 房屋
admin的 登录
以及错误页

添加了userService的getUser()获取当前用户信息

获取用户当前username，user详细信息接口已经完成

已经测试的接口：
getuser
adminquery
各级地址 support/*
updateCover
houseEditInfo
showRentHouse simpleQuery

因为代价过大，
要使用kafka吗？暂时不考虑

现在是将图片传到本地
未来部署nginx以后要把路径改成nginx的静态资源文件路径
同时修改在convertutil houseservice的cdnprefix参数
还要换数据库的图片路径（nginx的）

但是应该在前端开发完成以后，，，

麻了 elk感觉应该是所有都做完以后实现，，，
甚至是在部署完以后实现

首先 完成中文分词

然后，部署nginx 把图片功能调通 数据库字段改好（初步看起来只需要house表修改cover）

然后 测试所有接口 所有接口测试已经完成

尝试图片上传功能,修改界面不改图片（或者也是重新上传图片） 仅上传时上传图片

然后，完成前端（两周左右）

分别进行部署
调通接口 （一周）

最后进行elk（一周）

需要的页面：
用户登录页 （已完成）
用户注册页
管理员登录页
用户搜索页
用户中心页
房屋详情页
管理员自身房屋搜索页
管理员编辑房屋详情页
